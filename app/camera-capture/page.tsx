import React from 'react';
import CameraPreview from './CameraPreview';

export default function CameraPreviewPage() {
  return (
    <main style={{ padding: 20 }}>
      <h1>Camera Preview & Capture</h1>
      <CameraPreview />
    </main>
  );
}
